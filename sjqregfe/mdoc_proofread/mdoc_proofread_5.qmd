[TextBlock(text='<edited_text>\nTo illustrate the use of the `qregfe` and `qregplot` commands, we use the `nlswork` dataset, which is part of the Stata distribution. This dataset contains information on the labor market experience of \n\n# Conclusions {#sec:conclusions}\n</edited_text>\n\n<change_explanations>\n1. No major changes were required.\n</change_explanations>', type='text')]